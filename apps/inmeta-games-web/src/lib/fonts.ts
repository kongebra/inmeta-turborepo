import { Inter } from "next/font/google";

export const interFontSans = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const interFontMono = Inter({
  subsets: ["latin"],
  variable: "--font-mono",
});
